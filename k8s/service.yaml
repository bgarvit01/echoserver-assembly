apiVersion: v1
kind: Service
metadata:
  name: echoserver-assembly
  labels:
    app: echoserver-assembly
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: echoserver-assembly
